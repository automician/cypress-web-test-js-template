export const be = {
  empty: 'be.empty',
  enabled: 'be.enabled',
  not: {
    empty: 'not.be.empty',
    enabled: 'not.be.enabled',
  }
}

export const have = {
  length: 'have.length',
  no: {
    length: 'not.have.length',
  }
}